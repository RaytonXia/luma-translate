# 更新日志

本项目的版本号遵循 `主版本.次版本.修订号` 格式。

## v4.1.1 — 紧急修复

AI 设置窗口的根布局声明了 `GrowStyle = FixedSize` 却未设置 `ColumnCount`，可用单元格数为 8 行 × 0 列 = 0，添加第一个控件即抛出「TableLayoutPanel 已满并且 GrowStyle 为 FixedSize」的未处理异常。该窗口因此完全无法打开，导致 DeepSeek 与 Gemini 的密钥和隐私授权都无法配置，AI 功能整体不可用。现已补上 `ColumnCount = 1`，并复查了全部布局面板的容量。

---

## v4.1

**修复**

- 使用 Windows OneCore 本地语音时，「发音 / 听解释」按钮在第一次查询后被错误禁用
- AI 长句翻译失败时，气泡停留在「AI 正在翻译…」而错误只出现在悬停提示中；现在失败原因直接显示在气泡内
- AI 设置窗口每次打开都要求重新勾选隐私同意；现在会记住每个服务已保存的同意状态
- AI 设置窗口回车键无法保存
- 气泡头部的音标在词性与来源标签都较宽时可能与来源标签重叠
- 从托盘启动时主窗口可能闪现一帧
- 停止朗读的代码路径依赖被吞掉的空引用异常

**清理**

- 移除约 700 行从未被调用的历史代码（旧版布局、旧版 Gemini 单服务设置窗口、未接线的剪贴板取词流程）

**视觉**

- 鼠标光点重绘为带呼吸光晕与环绕光点的灵动光珠；等待第二次右键时显示节拍双点，识别中为薄荷绿到淡紫的三点波浪，AI 选区时为渐变 AI 徽章
- 光点跟随增加轻微阻尼，动画在高分屏按显示器缩放渲染
- AI 选区改为圆角渐变色带并附内侧细描边
- 词典气泡以 120 毫秒淡入上浮出现，按钮增加按压反馈，长句译文的可见行数增加
- 控制中心与设置窗口统一了配色、圆角与间距，整体更紧凑
